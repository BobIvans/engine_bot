version: "0.0"
strategy_name: "walk-forward-fixture"

run:
  timezone: "Europe/Riga"
  mode: "paper"

# Required subtrees for integration.config_loader.load_params_base
wallet_profile: {}

token_profile:
  gates: {}
  honeypot:
    enabled: false

signals: {}

risk: {}

execution: {}

# PR-D.1 walk forward minimal config
min_edge_bps: 0
modes:
  default:
    tp_pct: 0.05
    sl_pct: -0.05
    hold_sec_max: 10
