version: "0.0"
strategy_name: "daily-metrics-mixed-exits"

run:
  timezone: "Europe/Riga"
  mode: "paper"

wallet_profile: {}

token_profile:
  gates: {}
  honeypot:
    enabled: false

signals: {}

risk: {}

execution: {}

# Config for mixed exits case - min_edge_bps=0 so all trades ENTER
min_edge_bps: 0
modes:
  U:
    tp_pct: 0.05
    sl_pct: -0.05
    hold_sec_max: 30
